# Test Environment Configuration

# API Configuration
REACT_APP_API_BASE_URL=http://test-api.example.com/api
REACT_APP_API_TIMEOUT=10000
REACT_APP_API_VERSION=v1
REACT_APP_API_RETRY_ATTEMPTS=1

# Test specific settings
NODE_ENV=test
CI=true  # Ensures tests run in CI mode
SKIP_PREFLIGHT_CHECK=true  # Skip preflight checks in test environment